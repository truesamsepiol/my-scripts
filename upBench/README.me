# Schedulers for prim

I write a single and full scheduler for manage upmem apps.

## Usage

Pour executer un scheduler vous devez lui fournir des traces que vous avez généré au préalable grace au générateur de trace que vous trouvez dans les dossiers single et full

```bash
./generate_trace -h

./full_scheduler -h

./single_scheduler -h
```

Si jamais vous modifiez du code, vous pouvez vous servir du script commandes.sh pour tout recompiler. 
